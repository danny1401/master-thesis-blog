/* =========================================================
   1. CHART CONFIGURATION
   Change these values to customize the visualization.
   ========================================================= */
  
export const chartConfig = {
  // ---------- Size ----------
  chartHeight: 500,
  barHeight: 50,
  labelWidth: 100,

  // ---------- Axis ----------
  axisTicks: [0, 25, 50, 75, 100],

  // ---------- Bar ----------
  barRadius: 6,

  // ---------- Text ----------
  labelColor: "var(--secondary)",
  axisColor: "var(--secondary)",

  // ---------- Chart ----------
  barColor: "var(--primary)",
  barHoverColor: "var(--primary-accent)",
  barInactiveOpacity: 0.2,

  // ---------- Grid ----------
  gridColor: "var(--foreground)",
};
